Draft ontology entities are stored here during recovery and review.

Structure:

- `signals/<id>.json`
- `failure_modes/<id>.json`
- `response_patterns/<id>.json`
- `blocks/<id>.json`
- `tools/<id>.json`

Only reviewed drafts are eligible for publication into a new ontology version.
